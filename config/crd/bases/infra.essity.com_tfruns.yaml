---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: tfruns.infra.essity.com
spec:
  group: infra.essity.com
  names:
    kind: TfRun
    listKind: TfRunList
    plural: tfruns
    singular: tfrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.workspaceID
      name: Workspace
      type: string
    - jsonPath: .status.activeJobName
      name: Job
      priority: 1
      type: string
    - jsonPath: .status.lastRunTime
      name: LastRun
      type: date
    - jsonPath: .status.message
      name: Message
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TfRun is the Schema for the tfruns API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TfRunSpec defines the desired state of TfRun
            properties:
              arguments:
                additionalProperties:
                  type: string
                type: object
              backend:
                properties:
                  cloud:
                    default:
                      hostname: essity.scalr.io
                      organization: my-org
                      provider: scalr
                      workspace: my-workspace
                    description: Cloud backend configuration
                    properties:
                      agentPoolId:
                        type: string
                      environmentId:
                        type: string
                      hostname:
                        type: string
                      organization:
                        type: string
                      provider:
                        default: scalr
                        enum:
                        - scalr
                        - terraformCloud
                        type: string
                      workspace:
                        type: string
                    required:
                    - hostname
                    - organization
                    - provider
                    - workspace
                    type: object
                  storageAccount:
                    properties:
                      accountName:
                        type: string
                      containerName:
                        type: string
                      key:
                        type: string
                    required:
                    - accountName
                    - containerName
                    - key
                    type: object
                type: object
              engine:
                properties:
                  type:
                    default: opentofu
                    enum:
                    - terraform
                    - opentofu
                    type: string
                  version:
                    default: latest
                    type: string
                type: object
              forProvider:
                properties:
                  credentialsSecretRef:
                    type: string
                required:
                - credentialsSecretRef
                type: object
              source:
                description: TfSource defines the source of the Terraform configuration
                properties:
                  credentialsSecretRef:
                    description: CredentialsSecretRef is the name of the secret containing
                      git credentials
                    type: string
                  module:
                    description: Module is the git URL of the Terraform module
                    type: string
                  path:
                    description: Path is the path within the repository to the Terraform
                      configuration
                    type: string
                  ref:
                    description: Ref is the git reference (branch, tag, commit)
                    type: string
                required:
                - module
                type: object
              vars:
                x-kubernetes-preserve-unknown-fields: true
            required:
            - forProvider
            - source
            type: object
          status:
            description: TfRunStatus defines the observed state of TfRun
            properties:
              activeJobName:
                description: ActiveJobName is the name of the currently running Job
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the TfRun's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastRunTime:
                description: LastRunTime is the timestamp of the last run
                format: date-time
                type: string
              lastSpecHash:
                description: LastSpecHash is the hash of the spec from the last successful
                  run
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed TfRun
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the TerraformRun
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              workspaceID:
                description: WorkspaceID is the Scalr workspace ID created for this
                  TfRun
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
